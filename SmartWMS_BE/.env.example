# SmartWMS Environment Variables
# Copy this file to .env and fill in your values

# JWT Authentication (REQUIRED in production)
# Generate a secure key: openssl rand -base64 32
JWT_SECRET_KEY=your_secure_secret_key_here_at_least_32_characters

# Database Connection (override appsettings.json)
# CONNECTION_STRINGS__DEFAULTCONNECTION=Host=localhost;Port=5432;Database=smartwms;Username=postgres;Password=your_password

# CORS Origins (comma-separated)
# CORS__ALLOWEDORIGINS__0=https://your-frontend.com
